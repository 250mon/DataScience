# exclude everything except .py

# Ignore everything
*

# But not these files
!/.gitignore
!*.py
!*.yaml
!*.txt

# even if they are in subdirectoreis
!*/

# if the files to be tracked are in subdirectories
